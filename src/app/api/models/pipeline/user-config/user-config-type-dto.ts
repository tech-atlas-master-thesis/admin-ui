export enum UserConfigTypeDto {
  STRING = 'STRING',
  INTEGER = 'INTEGER',
  FLOAT = 'FLOAT',
  LIST = 'LIST',
  MAPPING = 'MAPPING',
  ENUM = 'ENUM',
  DATE = 'DATE',
  PIPELINE = 'PIPELINE',
  STEP = 'STEP',
  CONFIGURATION = 'CONFIGURATION',
}
